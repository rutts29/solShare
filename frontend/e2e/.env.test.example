# E2E Test Environment Configuration
# Copy this file to .env.test and fill in values for integration testing

# Mock mode (fast, no real transactions)
# Set to "false" for integration tests with real backend
MOCK_MODE=true

# Frontend URL for tests
TEST_FRONTEND_URL=http://localhost:3000

# Backend API URL (for integration mode)
TEST_API_URL=http://localhost:3001/api

# Test wallet for integration mode (devnet only!)
# Generate a new keypair for testing: solana-keygen new --no-bip39-passphrase
TEST_WALLET_PRIVATE_KEY=
TEST_WALLET_ADDRESS=

# Supabase (for integration tests)
# Use test/staging project, never production!
TEST_SUPABASE_URL=
TEST_SUPABASE_ANON_KEY=
