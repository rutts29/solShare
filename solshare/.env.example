# Solana Configuration
# Set via: solana config set --url <url>

# Network (devnet, testnet, mainnet-beta)
ANCHOR_PROVIDER_URL=https://api.devnet.solana.com

# Wallet path
ANCHOR_WALLET=~/.config/solana/id.json

# Program IDs (will be generated on first deploy)
# These are set automatically during anchor deploy
# SOCIAL_PROGRAM_ID=
# PAYMENT_PROGRAM_ID=
# TOKEN_GATE_PROGRAM_ID=
